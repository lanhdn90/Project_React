import React from 'react';
import PropTypes from 'prop-types';

School.propTypes = {
    
};

function School(props) {
    return (
        <div>
            Hello School
        </div>
    );
}

export default School;